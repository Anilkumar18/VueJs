# Install npm above version 6
# Install node above version 10
# For package version reference go through the package.json
# Copy the source files
# Check the npm (npm -v) and node (node -v)version before compile
# Execute "npm install"
# Execute "npm run dev"